services:
  dashboard:
    build: .
    container_name: ipad-dashboard
    restart: unless-stopped
    ports:
      - "3456:3000"
    env_file:
      - .env
    networks:
      - Userbridge

networks:
  Userbridge:
    external: true
